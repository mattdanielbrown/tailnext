# Todas las variables son opcionales: el template funciona sin ninguna.

# Sobrescribe el origen canónico del sitio, que por defecto sale de src/config.ts.
# Útil en despliegues de vista previa y en forks, donde el dominio no es el del template.
# NEXT_PUBLIC_SITE_ORIGIN="https://example.com"
